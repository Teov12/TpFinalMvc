@model TpFinalProductos.Models.TipoDeVehiculo

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <h4>Tipo de vehiculo</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.vehiculo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.vehiculo)
        </dd>
    </dl>
</div>
<div>
    <button class="btn btn-primary"><a style="color:white" asp-action="Edit" asp-route-id="@Model.Id">Editar</a></button>
    <button class="btn btn-secondary"><a style="color:white" asp-action="Index">Volver a la lista</a></button>
</div>
